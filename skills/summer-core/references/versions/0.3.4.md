# Summer 0.3.4 — summer-core

Released: 2026-05-08

No summer-core surface changes. The release is focused on `summer-security` (SSE filter, `ProviderJwtDecoderResolver`, `KeycloakRoleSynchronizer` cleanup). See `summer-security/references/versions/0.3.4.md` for full detail.

## Cross-cutting

- `summer-platform` BOM bumps to `0.3.4`; `paymentSdkVersion` stays at `0.3.3` for this release.
- No DB / schema changes.
- No `summer-core` API additions, deprecations, or removals.

## Migration from 0.3.3

Bump the BOM only. No core code changes.

## See also

- `summer-security/references/versions/0.3.4.md` — `SseQueryParamTokenFilter`, `SseAuthCustomizer`, `ProviderJwtDecoderResolver`, `KeycloakRoleSynchronizer` no-null-bean.
- `summer-core/references/migrations/0.3.3-to-0.3.4.md` — service-side migration (delete service-local SSE filter copies).
