# Summer 0.2.1 — summer-test

Released: 2026-02-26

## Features

- Added 12 unit tests for `AuditServiceIml` covering builder fallbacks, actor extraction, and
  Mono/Flux audit aspect behaviours. Useful as a reference template for testing reactive audit
  flows in services that consume `AuditService`.

## Breaking changes

None for `summer-test`.

## Migration from 0.2.0

No changes required. If you maintain a `MockAuditService` in your test fixtures, mirror the
new `audit(AuditLog)` builder method (added in `summer-data` 0.2.1 — see that overlay).
