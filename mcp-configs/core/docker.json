{
  "_comment": "Docker MCP — container lifecycle, image management, log inspection. Essential for Testcontainers workflows.",
  "_install": "claude mcp add docker -s user -- npx -y @modelcontextprotocol/server-docker",
  "_env_required": [],
  "_env_example": {},
  "_security": "Accesses the local Docker daemon. Only use on development machines. Never expose to untrusted environments.",
  "_tools_added": 8,
  "mcpServers": {
    "docker": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-docker"]
    }
  }
}
