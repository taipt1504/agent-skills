{
  "_comment": "Kafka MCP — topic inspection, message production/consumption, consumer group management. Enable only when project uses Kafka.",
  "_install": "claude mcp add kafka -s user -e KAFKA_BROKERS -- npx -y @modelcontextprotocol/server-kafka",
  "_env_required": ["KAFKA_BROKERS"],
  "_env_example": {
    "KAFKA_BROKERS": "localhost:9092"
  },
  "_security": "Use dev/staging Kafka brokers only. Never point at production. Topic creation/deletion modifies cluster state.",
  "_tools_added": 8,
  "mcpServers": {
    "kafka": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-kafka"],
      "env": {
        "KAFKA_BROKERS": "${KAFKA_BROKERS:-localhost:9092}"
      }
    }
  }
}
