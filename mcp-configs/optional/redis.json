{
  "_comment": "Redis MCP — cache inspection, key browsing, data structure access. Enable only when project uses Redis.",
  "_install": "claude mcp add redis -s user -- npx -y @modelcontextprotocol/server-redis ${REDIS_URL:-redis://localhost:6379}",
  "_env_required": [],
  "_env_example": {
    "REDIS_URL": "redis://localhost:6379"
  },
  "_security": "Connect to LOCAL/DEV Redis only. Never point at production. Read operations are safe; write operations modify cache state.",
  "_tools_added": 8,
  "mcpServers": {
    "redis": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-redis", "${REDIS_URL:-redis://localhost:6379}"]
    }
  }
}
